# Flask API server (predict_api.py)
flask>=3.0
flask-cors>=4.0

# Core data & ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Visualization
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# Dashboard
streamlit>=1.28

# Explainability (optional — code falls back to permutation importance
# and built-in feature_importances_ if this isn't installed)
shap>=0.43

# Optional stronger gradient boosting (code falls back to sklearn's
# GradientBoostingClassifier automatically if these aren't installed)
xgboost>=2.0
# lightgbm>=4.0

# Testing
pytest>=7.4

# Notebooks
jupyter>=1.0
notebook>=7.0

# Streamlit login — MySQL auth (same DB as PHP frontend)
mysql-connector-python>=8.3
bcrypt>=4.1
